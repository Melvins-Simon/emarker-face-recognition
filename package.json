{
  "name": "auth",
  "version": "1.0.0",
  "description": "authentication and authorization with express and nodejs",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=production node --env-file=.env server/server.js",
    "server": "NODE_ENV=development node --watch --trace-warnings --env-file=.env server/server.js",
    "build":"npm install && npm install --prefix client && npm run build --prefix client"
  },
  "author": "melvinssimon",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.6.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.11.0",
    "nodemailer": "^6.10.0"
  }
}
